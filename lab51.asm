DATA SEGMENT PARA PUBLIC 'DATA'
DATA ENDS
CODE SEGMENT PARA PUBLIC 'CODE'
ASSUME CS:CODE, DS:DATA
START PROC FAR
PUSH DS 
XOR AX,AX
PUSH AX
MOV AX,DATA
MOV DS, AX

MOV AX, 20
MOV BX, 15
MOV CX, AX
SHL AX, 1
ADD CX, AX
SHL AX, 1
ADD CX, AX
SHL BX, 1
MOV AX, CX
SUB AX, BX
SHR BX, 4
SUB AX, BX

RET
START ENDP

CODE ENDS

END START