DATA SEGMENT PARA PUBLIC 'DATA'
NRS DB 8 DUP(?)
DATA ENDS
CODE SEGMENT PARA PUBLIC 'CODE'
START PROC FAR
ASSUME CS:CODE, DS:DATA
PUSH DS 
XOR AX,AX
PUSH AX
MOV AX,DATA; 
MOV DS, AX
XOR SI,SI
MOV AX, 120
MOV CX, 10
NEXT1:
CWD 
DIV CX 
MOV NRS[SI], DL
ADD NRS[SI], '0'
INC SI
CMP AX, 0
JNE NEXT1

MOV CX, SI
MOV AH, 02H 
DEC SI

NEXT2: MOV DL, NRS[SI]
INT 21H
DEC SI
LOOP NEXT2

 
RET
START ENDP

CODE ENDS

END START