DATA SEGMENT PARA PUBLIC 'DATA'
ALMA DB 1
STRING DW “6359”
LEN DW $-STRING
ADD1 DD STRING
DATA ENDS

DATA2 SEGMENT PARA PUBLIC 'DATA'
SUML DW ?
DATA2 ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
ASSUME CS:CODE

SUM PROC FAR
MOV AX, DATA2
MOV ES, AX
ASSUME ES:DATA2

MOV SUML, 0
MOV DS, DX
XOR AX, AX
XOR SI, SI

label1:
MOV AL, DS:BX[SI]
SUB AL, ‘0’
ADD SUML, AX
INC SI
LOOP label1

MOV AX, SUML
RET

SUM ENDP

START PROC FAR
ASSUME DS:DATA
PUSH DS
XOR AX, AX
PUSH AX
MOV AX, DATA
MOV DS, AX

LDS BX, ADD1
MOV CX, LEN
MOV DX, DS

CALL SUM

RET

START ENDP

CODE ENDS
END START